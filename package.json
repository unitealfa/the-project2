{
  "name": "the-project2",
  "version": "1.0.0",
  "private": true,
  "description": "Application interne de gestion des commandes, produits et livraisons ECOTRACK",
  "scripts": {
    "dev:back": "npm --prefix back run dev",
    "dev:front": "npm --prefix front run dev",
    "build": "npm --prefix back run build && npm --prefix front run build",
    "test": "npm --prefix back test && npm --prefix front run build",
    "audit": "npm --prefix back audit && npm --prefix front audit"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
